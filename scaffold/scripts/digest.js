/**
 * digest.js — STUB (structural twin)
 * Intended function: Daily and weekly digest generator from state/ and logs/.
 * Original pattern:  17:00 weekdays daily; Friday weekly rollup; output to state/weekly-reports/.
 * Secrets:           fetched at runtime from Key Vault via managed identity
 *                    (see /opt/twin-bootstrap/fetch-secret.sh) — never from disk.
 * Status:            NOT IMPLEMENTED. Logs invocation and exits.
 */
console.error('[stub] digest.js invoked — not implemented');
process.exit(0);
