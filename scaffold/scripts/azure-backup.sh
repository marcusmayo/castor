#!/usr/bin/env bash
# azure-backup.sh — STUB (structural twin)
# Intended function: Daily encrypted backup of the working tree to Azure Blob Storage.
# Original pattern:  02:00 UTC; auth via managed identity (azcopy login --identity) — no storage account keys on disk.
# Status:            NOT IMPLEMENTED. Logs invocation and exits.
set -euo pipefail
echo "[stub] azure-backup.sh invoked — not implemented" >&2
exit 0
